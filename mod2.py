import random
from mod1 import get_student

def get_teacher(n):
    t = []
    for i in range(n):
        te = input("Enter teacher name : ")
        t.append(te)
    print(t)

    for i in range(3):
        x =  get_student(3)
        str1.join(x)
        for j in range(len(x.split(' '))):
            t[0] += ":" + str[i]
    print(t[0])

get_teacher(2)
